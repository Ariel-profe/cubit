export interface IFooterLinks {
    title: string;
    links: {
        id: number;
        title: string;
        href: string;
    }[]
};

export interface ISocialMediaLinks {
    icon: string;
    title: string;
    href: string;
};